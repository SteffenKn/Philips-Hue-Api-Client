export type LightgroupState = {
  on: boolean,
  bri: number,
  hue: number,
  sat: number,
  effect: string,
  xy: [
    number,
    number
  ],
  ct: number,
  alert: string,
  colormode: string,
};
